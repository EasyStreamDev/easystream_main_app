import { Box, Typography } from "@mui/material";
import React from "react";

export default function BoxEvent(props: any) {

  // const printKeywords = () => {
  //   props.keyword.map((item: any, index: number) => {
  //     const elem = item;
  //     return <h1>{item.id}</h1>
  //   })
  // }


  return (
    <>
      <Box>
        {/* <h1>{props.keyObj.id}</h1> */}
        {/* {props.keyObj.keyword.map((item: any, index: number) => { */}
          {/* // return <h1>{item}</h1> */}
        {/* })}; */}
      </Box>
    </>
  );
}
