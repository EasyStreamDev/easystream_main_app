name: CI Electron test

on:
  pull_request:
    branches: [feature-github-actions]
  push:
    branches: [feature-github-actions]

jobs:
  macosBuild:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install requirements and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl tar gnupg2 rpm dpkg fakeroot
          curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
          echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
          sudo apt-get update
          sudo apt-get install -y yarn
          echo Node $(node --version)
          echo Yarn $(yarn --version)
          yarn
          yarn global add @electron-forge/cli
          yarn global add ynpx
          yarn global add node-gyp@latest
          yarn config set node_gyp "/usr/local/lib/node_modules/node-gyp/bin/node-gyp.js"
      - name: Build commit ${{ github.sha }}
        run: |
          yarn make-mac
      - name: Test commit ${{ github.sha }}
        run: |
          yarn test
    # runs-on: macos-latest
    # steps:
    #   - name: Checkout
    #     uses: actions/checkout@v2
    #   - name: Install requirements and dependencies
    #     run: |
    #       brew update
    #       brew upgrade
    #       brew install node
    #       brew install yarn
    #       echo Node $(node --version)
    #       echo Yarn $(yarn --version)
    #       yarn
    #       yarn global add @electron-forge/cli
    #       yarn global add ynpx
    #       yarn global add node-gyp@latest
    #       yarn config set node_gyp "/usr/local/lib/node_modules/node-gyp/bin/node-gyp.js"
    #   - name: Build commit ${{ github.sha }}
    #     run: |
    #       yarn make-mac
    #   - name: Test commit ${{ github.sha }}
    #     run: |
    #       yarn test
  linuxBuild:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install requirements and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl tar gnupg2 rpm dpkg fakeroot
          curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
          echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
          sudo apt-get update
          sudo apt-get install -y yarn
          echo Node $(node --version)
          echo Yarn $(yarn --version)
          yarn
          yarn global add @electron-forge/cli
          yarn global add ynpx
      - name: Build commit ${{ github.sha }}
        run: |
          yarn make-linux
      - name: Test commit ${{ github.sha }}
        run: |
          yarn test
  win32Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install requirements and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl tar gnupg2 rpm dpkg fakeroot
          curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
          echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee /etc/apt/sources.list.d/yarn.list
          sudo apt-get update
          sudo apt-get install -y yarn
          echo Node $(node --version)
          echo Yarn $(yarn --version)
          yarn global add @electron-forge/cli
          yarn global add ynpx
          yarn
          sudo apt update
          sudo apt install dirmngr gnupg apt-transport-https ca-certificates software-properties-common
          sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 3FA7E0328081BFF6A14DA29AA6A19B38D3D831EF
          sudo apt-add-repository 'deb https://download.mono-project.com/repo/ubuntu stable-focal main'
          sudo apt install mono-complete
          sudo add-apt-repository main
          sudo add-apt-repository universe
          sudo add-apt-repository multiverse
          sudo apt update
          sudo apt-get dist-upgrade -y
          sudo apt-get install -y lib32z1
          sudo apt-get install -y libgtk2.0-0:i386 libidn11:i386 libglu1-mesa:i386 libxmu6:i386
          sudo apt-get install -y libpangox-1.0-0:i386 libpangoxft-1.0-0:i386
          sudo dpkg --add-architecture i386
          sudo apt-get install -y wine32
      - name: Build commit ${{ github.sha }}
        run: |
          yarn make-win32
      - name: Test commit ${{ github.sha }}
        run: |
          yarn test